<h1>Your Cart</h1>
  <%=render "cart/orderables"%>
<% @cart.orderables.each do |orderable| %>
  <% electronic = orderable.electronic %>
  <p><%= electronic.name %> * <%= orderable.quantity %> = $<%= electronic.price * orderable.quantity %></p>
<% end %>
<p>Your total is $<%= @cart.total %> </p>